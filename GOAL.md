# Project goal

Last updated: July 20, 2026

Build a dependable, evidence-first radio archive intelligence application that is pleasant for an ordinary user on Windows and has a credible path to the same core workflow on Linux and macOS.

The tested Windows experience remains the reference behavior. A user should be able to discover relevant feeds, acquire a quota-safe date range, see retained work in a local library, resume only the missing processing stages, inspect transcripts and speaker labels, review evidence-backed incidents and clips, and ask for daily, weekly, or regional summaries without understanding the pipeline internals.

## Current acceptance targets

1. **Pipeline parity:** CUDA, CPU, Vulkan, OpenVINO, and Windows ML profiles must each report stage-by-stage readiness honestly. A profile is called ready only after real transcription, diarization/fallback, and local-analysis checks appropriate to that profile pass.
2. **Resumability:** downloads, combination, transcription, diarization, embeddings, incident extraction, and summaries persist independently. Incident extraction checkpoints every completed model window, and the portable speaker preview atomically checkpoints every completed audio chunk. Its checkpoint remains until the final diarization cache is safely committed, so an interruption resumes inside either long-running stage instead of discarding earlier work. A retry starts at the first missing or invalid stage; an evidence-rule upgrade reuses retained audio/transcripts while withholding older claims until local reanalysis finishes.
3. **Evidence:** every surfaced event can be traced to timestamped transcript text and a playable/exportable local clip. Display serializers must apply the same bounded citation window and deterministic public-text redaction instead of returning raw SQLite evidence. Missing coverage is never described as inactivity.
4. **Quota safety:** archive acquisition first reuses exact verified source blocks available from the user's trusted-LAN peers, then remains sequential, paced, cache-aware, responsive to `Retry-After`, and stops all further website media requests after the explicit Broadcastify download-limit response.
5. **Usable UI:** primary workflows use clear navigation and master/detail views rather than one long page. Common actions fit at the reference 1240x900 window size; advanced hardware controls stay optional.
6. **Portable providers:** local engines remain the default, with explicit provider contracts for supported OpenAI-compatible APIs or an authenticated Codex CLI harness. Secrets must not be persisted in ordinary settings or committed.
7. **Cross-platform direction:** the Python service and browser UI expose the same library/review/action contract on Windows, Linux, and macOS. Loopback remains the default; explicit trusted-LAN hosting and a persistent TrueNAS App provide the headless/NAS surface, while the native WinUI app remains the polished Windows shell. Real-machine Linux full-model, LAN-hosted TrueNAS Apps, and visual/accessibility validation are complete; real macOS validation and non-developer packaging are required before calling portability complete.
8. **Reviewable delivery:** substantial sections are committed and pushed separately, with tests, real-machine evidence, limitations, and security checks recorded in `PROGRESS.md` and `BUGS.md`.

## Definition of done for the active parity phase

- CUDA reference flow remains green on the RTX 3090.
- CPU transcription, CPU diarization, and CPU LLM fallback are exercised; exact commit `historical-validation` completes the protected all-CPU Web workflow with grounded persistence. Community-1 remains the accuracy-default diarizer, while a separately labeled sherpa-onnx preview supplies a much faster public-model CPU path that can later be upgraded without repeating ASR. A retained 23.9-hour feed day completed that speaker stage in 1,951.971 seconds (0.0227 real-time factor); full-day all-CPU ASR and LLM timing remains a separate open benchmark.
- OpenVINO performs a real Whisper decode and safely falls back to CPU when an exposed accelerator rejects the model. Exact source commit `historical-validation` now completes a fresh protected 60-second Web job through CPU diarization, OpenVINO CPU ASR, local Gemma, embeddings, persistence, and Ready-to-review state in 31 seconds.
- Vulkan performs a real whisper.cpp decode and llama.cpp generation on suitable AMD or Intel hardware; exact commit `historical-validation` passes a protected loopback Web job through network-disabled AMD Radeon 890M ASR → CPU diarization → local analysis. Exact commit `historical-validation` passes all 212 tests in the hardened appliance container and completes the current preview profile on that Radeon in 9.199 seconds: Vulkan ASR 2.547 seconds, sherpa-onnx CPU preview 0.321 seconds, and Vulkan analysis 5.626 seconds. Its five-minute retained-radio preview returned 19 turns in 6.634 seconds, and its 23.9-hour feed-day run returned 3,893 turns in 1,951.971 seconds—44.05 times faster than real time. Against the retained Community-1 comparison it covered 91.05% of reference speech and 93.23% of timestamped word midpoints, kept 97.62% of its speech inside the reference, and produced zero duplicate ownership at 95 chunk boundaries. Exact commit `historical-validation` then passed 219 hardened tests and a forced real-process interruption: the resumed run reused chunk 1/5, completed the remaining chunks, committed the final cache, and removed its checkpoint only afterward. Vulkan/OpenVINO/Windows ML/Metal/Qwen presets select this opt-in preview for faster portable completion, keep anonymous IDs scoped to bounded chunks, and expose a one-click Community-1 upgrade that never repeats transcription. This is end-to-end profile parity, not a claim that those GPU APIs accelerate diarization or that Community-1 is human ground truth.
- Windows ML performs a real ONNX Whisper decode through the packaged helper and is integrated only after the model/runtime self-test passes. Exact source commit `historical-validation` completes the matching fresh protected 60-second Web job in 32 seconds and an idempotent resume without repeating ASR, diarization, analysis, or embeddings. Managed model preparation now verifies the exact engine/model/path/provider identity instead of accepting an unrelated cached graph. The retained-radio starter is multilingual Whisper Base on CPU: Tiny passed clean-speech execution but returned `[inaudible]` on the low-SNR event fixture, while Base captured its core dispatch in 0.212 seconds. The generated-silence UI test proves execution only; GPU catalog entries remain gated until a timed compatible-model decode proves acceleration.
- Whisper remains the validated evidence path while newer local ASR is evaluated against retained radio, not public clean-speech scores alone. Qwen3-ASR 0.6B INT8 through sherpa-onnx is now an implemented opt-in fast-CPU profile with checksum-verified managed acquisition, atomic installation, exact runtime identity, English control, bounded Community-1/Silero source-region timestamps, and no fabricated word timing. It preserved the core meaning of 13 retained evidence clips at about 0.08 real-time factor, and its real Qwen/Community-1/Gemma joined Windows proof completed in 15.265 seconds. Exact pushed source also installed and ran on AMD/Linux, processing a five-minute slice in 7.128 seconds at four threads; the same run's `shops fired` / `nothing seems` drift keeps longer-corpus critical-word/false-claim gates open before any default change. The separate forced aligner is fast on CUDA and sub-real-time on CPU, but it timestamps supplied wrong words without mismatch confidence, so it remains a possible non-evidentiary seek aid rather than a promotion gate.
- Current unified ASR/diarization models are evaluated separately. Microsoft VibeVoice-ASR 8B BF16 directly produced speaker/timestamp/content records and preserved the retained `shots fired`, fire, and stolen-squad-car events on the RTX 3090, but required a 15.53 GiB snapshot/16.58 GiB peak allocation. Across the retained fixtures it hallucinated short noise, changed an address, omitted a disregard, and merged two Community-1 speakers. It is the leading high-end unified research candidate, not an evidence-validated default; an isolated runtime and broader long-window scoring are required.
- Settings explain the selected stage backends, consolidate account/storage/transcription/speaker/analysis readiness, offer one sequential **Verify profile** action plus individual real-execution stage tests in both UIs, and distinguish detected/configured capability from session-specific verified execution. Every detected or failed profile exposes a structured first recovery action that distinguishes a missing runtime/helper/dependency from a missing managed model and from a model that is present but fails execution. Model preparation stays hidden until its runtime exists. Unsupported platform choices are hidden, incompatible whisper.cpp model presets are repaired explicitly, and untested providers or CPU fallbacks cannot look like the requested accelerator succeeded.
- Web Settings mirrors the native task model with persistent, deep-linked **Setup**, **Processing**, **Analysis & AI**, and **Account** sections. Setup actions route to and focus the exact control, keyboard tab navigation works, and only one bounded section is shown at a time on desktop or phone. Exact source `historical-validation` retains the clean desktop/390×844 layout, adds platform-filtered profile choices and joined verification, and passes all 171 tests on Windows and in the isolated AMD/Vulkan Linux container.
- The Local Library viewer makes unfinished diarization, analysis, and evidence-rule upgrades obvious, offers the exact next action, and never presents an older analysis version as current.
- Native settings, the last review day/area profile, operation logs, and model-window progress survive an application or machine interruption; a startup failure leaves an append-only local diagnostic rather than overwriting the previous one.
- Regional story review keeps the publication warning, coverage state, ranked lead list, and one selected evidence package visible as a bounded master/detail workspace in both UIs, including responsive phone navigation, rather than rendering every full story card in one long page. The model-written assignment narrative is secondary and collapsed by default. Current browser serialization re-sanitizes both retained area packages and daily incident quotes; the July 15–16 27-reference corpus audit found eight context-supported redactions and no residual strong name context.
- Linux has a wheel-packaged, loopback-default per-user service lifecycle validated from exact commit `historical-validation`, and exact commit `historical-validation` passes the full-model desktop/mobile visual and accessibility matrix plus all 148 host tests. Exact `historical-validation` runs the Web UI and Vulkan/CPU/Vulkan profile as a persistent TrueNAS App on the trusted LAN. Real macOS validation still prevents calling portability complete.
- The application receives another visual QA and accessibility pass after every material navigation/viewer change.

The active Windows/Vulkan/Intel/Windows-ML parity and UI phase was completed on July 18, 2026. The product remains iterative: B-001 keeps Windows ML GPU acceleration gated, B-004 retains full-day all-CPU timing, and B-005 retains real-Mac/keychain/packaging work without weakening the completed tested paths.
